function Home() {
  return <h1>Test 1</h1>
}

// export a function to outside of the file
// it's possible to export various function at same time
// when it's define to be the default, it'smeans that it is the index of functions
// from it, one component call other and build everithing that is needed.
export default Home;